import{useMemo,useState}from"react";import{Bot}from"lucide-react";const SAMPLE='User-agent: *\nDisallow: /admin\nAllow: /\nSitemap: https://example.com/sitemap.xml';export default function RobotsTool(){const[input,setInput]=useState(SAMPLE);const res=useMemo(()=>{const lines=input.split(/\r?\n/);const sitemaps:string[]=[];const groups:string[]=[];const issues:string[]=[];let ua='';lines.forEach((l,i)=>{const line=l.trim();if(!line||line.startsWith('#'))return;const [k,...rest]=line.split(':');const v=rest.join(':').trim();const key=k.toLowerCase();if(!v)issues.push(`Line ${i+1}: missing value`);if(key==='user-agent'){ua=v;groups.push(v)}else if(key==='sitemap')sitemaps.push(v);else if(!['allow','disallow','crawl-delay','host'].includes(key))issues.push(`Line ${i+1}: unknown directive ${k}`)});return{groups,sitemaps,issues,ua}},[input]);return <div className="flex h-full flex-col"><div className="flex gap-2 border-b p-3 text-sm"><Bot className="size-4"/><b>Robots.txt Helper</b></div><div className="grid flex-1 md:grid-cols-2"><textarea value={input} onChange={e=>setInput(e.target.value)} className="resize-none border-r bg-transparent p-3 font-mono text-sm"/><div className="space-y-3 overflow-auto p-4"><Box title="User-agents" items={res.groups}/><Box title="Sitemaps" items={res.sitemaps}/><Box title="Issues" items={res.issues.length?res.issues:['No basic issues found']}/></div></div></div>}function Box({title,items}:{title:string;items:string[]}){return <div className="rounded border p-3"><div className="mb-2 text-xs font-bold uppercase text-[var(--muted-foreground)]">{title}</div>{items.map((x,i)=><div key={i} className="font-mono text-xs">{x}</div>)}</div>}
