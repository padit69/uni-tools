import{useMemo,useState}from"react";import{Code2}from"lucide-react";import{CopyButton}from"@/components/tool/CopyButton";type Lang='html'|'css'|'js';function fmt(s:string,l:Lang){let x=s.trim();if(l==='html')return x.replace(/></g,'>\n<').replace(/(<\/[^>]+>)/g,'$1\n').split('\n').map((a,i)=>'  '.repeat(Math.max(0,(a.match(/<\//)?-1:0)+Math.min(i,4)))+a.trim()).join('\n');if(l==='css')return x.replace(/\{/g,' {\n  ').replace(/;/g,';\n  ').replace(/\}/g,'\n}\n').replace(/\n\s*\n/g,'\n');return x.replace(/;/g,';\n').replace(/\{/g,' {\n  ').replace(/\}/g,'\n}\n').replace(/,/g,', ')}function min(s:string){return s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\s+/g,' ').trim()}export default function BeautifierTool(){const[lang,setLang]=useState<Lang>('html');const[input,setInput]=useState('<div><span>Hello</span></div>');const[mode,setMode]=useState<'format'|'minify'>('format');const out=useMemo(()=>mode==='format'?fmt(input,lang):min(input),[input,lang,mode]);return <div className="flex h-full flex-col"><div className="flex justify-between border-b p-3"><span className="flex gap-2 text-sm"><Code2 className="size-4"/>HTML/CSS/JS Beautifier</span><div className="flex gap-2"><select value={lang} onChange={e=>setLang(e.target.value as Lang)} className="rounded border bg-transparent px-2 text-xs"><option>html</option><option>css</option><option>js</option></select><button onClick={()=>setMode(mode==='format'?'minify':'format')} className="rounded border px-2 text-xs">{mode}</button></div></div><div className="grid flex-1 md:grid-cols-2"><textarea value={input} onChange={e=>setInput(e.target.value)} className="resize-none border-r bg-transparent p-3 font-mono text-sm"/><pre className="overflow-auto p-3 font-mono text-sm"><CopyButton text={out}/>{out}</pre></div></div>}
